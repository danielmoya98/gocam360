"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.brnSwitchChangedEventRename = void 0;
const tslib_1 = require("tslib");
const devkit_1 = require("@nx/devkit");
const generator_1 = tslib_1.__importDefault(require("../../migrate-brn-switch-changed-event/generator"));
const healthchecks_1 = require("../healthchecks");
exports.brnSwitchChangedEventRename = {
    name: 'Switch changed event rename',
    async detect(tree, failure) {
        (0, devkit_1.visitNotIgnoredFiles)(tree, '/', (file) => {
            // if the file is a .ts or .htlm file, check for helm icons
            if (!file.endsWith('.ts') && !file.endsWith('.html')) {
                return;
            }
            const contents = tree.read(file, 'utf-8');
            if (!contents) {
                return;
            }
            // check if the legacy openedChange event is being used
            if (/<(brn-switch)[^>]*\(\s*changed\s*\)=/g.test(contents)) {
                failure('Switch is using the renamed changed event.', healthchecks_1.HealthcheckSeverity.Error, true);
            }
        });
    },
    fix: async (tree) => {
        await (0, generator_1.default)(tree, { skipFormat: true });
        return true;
    },
    prompt: 'Would you like to migrate brn-switch changed event rename?',
};
//# sourceMappingURL=brn-switch-changed-event-rename.js.map