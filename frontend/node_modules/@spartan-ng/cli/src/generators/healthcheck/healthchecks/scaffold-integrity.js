"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.scaffoldIntegrityHealthcheck = void 0;
const devkit_1 = require("@nx/devkit");
const tsconfig_1 = require("../../../utils/tsconfig");
const primitive_deps_1 = require("../../ui/primitive-deps");
const healthchecks_1 = require("../healthchecks");
// Primitives imported via `<importAlias>/<name>` that have no matching tsconfig path - referenced by a
// scaffolded component but never generated themselves, so the import can't resolve (#1618).
function findMissingPrimitives(tree, importAlias) {
    const paths = (0, tsconfig_1.readTsConfigPathsFromTree)(tree);
    const importRegex = new RegExp(`['"\`]${escapeRegExp(importAlias)}/([a-z-]+)['"\`]`, 'g');
    const missing = new Set();
    (0, devkit_1.visitNotIgnoredFiles)(tree, '/', (file) => {
        if (!file.endsWith('.ts'))
            return;
        const contents = tree.read(file, 'utf-8');
        if (!contents)
            return;
        for (const [, name] of contents.matchAll(importRegex)) {
            if (name in primitive_deps_1.primitiveDependencies && !(`${importAlias}/${name}` in paths)) {
                missing.add(name);
            }
        }
    });
    return missing;
}
function escapeRegExp(value) {
    return value.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}
exports.scaffoldIntegrityHealthcheck = {
    name: 'Scaffold integrity',
    detect(tree, failure, _skip, { importAlias }) {
        for (const primitive of findMissingPrimitives(tree, importAlias)) {
            failure(`"${primitive}" is imported from ${importAlias}/${primitive} but was never generated. ` +
                `Run "nx g @spartan-ng/cli:ui ${primitive}" to add it.`, healthchecks_1.HealthcheckSeverity.Error, false);
        }
    },
};
//# sourceMappingURL=scaffold-integrity.js.map