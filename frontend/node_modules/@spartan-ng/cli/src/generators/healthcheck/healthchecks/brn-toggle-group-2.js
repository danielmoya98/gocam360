"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.brainToggleGroup2Healthcheck = void 0;
const tslib_1 = require("tslib");
const devkit_1 = require("@nx/devkit");
const generator_1 = tslib_1.__importDefault(require("../../migrate-brn-toggle-group/generator"));
const healthchecks_1 = require("../healthchecks");
exports.brainToggleGroup2Healthcheck = {
    name: 'Brain Toggle Group',
    async detect(tree, failure) {
        (0, devkit_1.visitNotIgnoredFiles)(tree, '/', (file) => {
            // if the file is a .ts or .htlm file
            if (!file.endsWith('.ts') && !file.endsWith('.html')) {
                return;
            }
            // skip HlmToggleGroup itself
            if (file.endsWith('hlm-toggle-group.ts') || file.endsWith('hlm-toggle-group-item.ts')) {
                return;
            }
            const contents = tree.read(file, 'utf-8');
            if (!contents) {
                return;
            }
            if (contents.includes("'@spartan-ng/brain/toggle-group'") || contents.includes('brn-toggle-group')) {
                failure(`The <brn-toggle-group> component is deprecated. Please use the <hlm-toggle-group> instead.`, healthchecks_1.HealthcheckSeverity.Error, true);
            }
        });
    },
    fix: async (tree) => {
        await (0, generator_1.default)(tree, { skipFormat: true });
        return true;
    },
    prompt: 'Would you like to migrate brn-toggle-group to hlm-toggle-group?',
};
//# sourceMappingURL=brn-toggle-group-2.js.map