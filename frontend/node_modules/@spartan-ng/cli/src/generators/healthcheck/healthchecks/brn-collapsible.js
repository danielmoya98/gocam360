"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.brainCollapsibleHealthcheck = void 0;
const devkit_1 = require("@nx/devkit");
const generator_1 = require("../../migrate-collapsible/generator");
const healthchecks_1 = require("../healthchecks");
exports.brainCollapsibleHealthcheck = {
    name: 'Brain Collapsible',
    async detect(tree, failure) {
        (0, devkit_1.visitNotIgnoredFiles)(tree, '/', (file) => {
            // if the file is a .ts or .htlm file, check for helm icons
            if (!file.endsWith('.ts') && !file.endsWith('.html')) {
                return;
            }
            // skip HlmCollapsible itself
            if (file.endsWith('hlm-collapsible.ts') ||
                file.endsWith('hlm-collapsible-trigger.ts') ||
                file.endsWith('hlm-collapsible-content.ts')) {
                return;
            }
            const contents = tree.read(file, 'utf-8');
            if (!contents) {
                return;
            }
            if (contents.includes("'@spartan-ng/brain/collapsible'") || contents.includes('brn-collapsible')) {
                failure(`The <brn-collapsible> component is deprecated. Please use the <hlm-collapsible> instead.`, healthchecks_1.HealthcheckSeverity.Error, true);
            }
        });
    },
    fix: async (tree) => {
        await (0, generator_1.migrateCollapsibleGenerator)(tree, { skipFormat: true });
        return true;
    },
    prompt: 'Would you like to migrate brn-collapsible to hlm-collapsible?',
};
//# sourceMappingURL=brn-collapsible.js.map