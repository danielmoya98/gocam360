"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.brainToggleHealthcheck = void 0;
const tslib_1 = require("tslib");
const devkit_1 = require("@nx/devkit");
const generator_1 = tslib_1.__importDefault(require("../../migrate-toggle/generator"));
const healthchecks_1 = require("../healthchecks");
exports.brainToggleHealthcheck = {
    name: 'Brain Toggle',
    async detect(tree, failure) {
        (0, devkit_1.visitNotIgnoredFiles)(tree, '/', (file) => {
            // if the file is a .ts or .htlm file, check for brain toggle
            if (!file.endsWith('.ts') && !file.endsWith('.html')) {
                return;
            }
            // if it is the HlmToggle itself skip it.
            if (file.endsWith('hlm-toggle.ts')) {
                return;
            }
            const contents = tree.read(file, 'utf-8');
            if (!contents) {
                return;
            }
            if (contents.includes("import { BrnToggle } from '@spartan-ng/brain/toggle';") ||
                contents.includes('brnToggle')) {
                failure(`The brnToggle directive can be replace with hlmToggle instead.`, healthchecks_1.HealthcheckSeverity.Error, true);
            }
        });
    },
    fix: async (tree) => {
        await (0, generator_1.default)(tree, { skipFormat: true });
        return true;
    },
    prompt: 'Would you like to migrate brain toggle?',
};
//# sourceMappingURL=brn-toggle.js.map