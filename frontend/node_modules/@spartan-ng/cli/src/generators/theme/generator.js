"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = addThemeToApplicationGenerator;
const enquirer_1 = require("enquirer");
const get_project_names_1 = require("../../utils/get-project-names");
const add_theme_to_application_styles_1 = require("./libs/add-theme-to-application-styles");
const colors_1 = require("./libs/colors");
async function addThemeToApplicationGenerator(tree, options = {}) {
    const setupTailwindCss = options.setupTailwindCss ?? true;
    // Passing a theme opts into non-interactive mode: every remaining answer falls back to a sensible
    // default (auto-detected styles entry point, global theme) instead of prompting. This lets the CLI be
    // driven by flags (`ng g @spartan-ng/cli:ui-theme --theme=zinc`) and scripted in CI.
    const nonInteractive = !!options.theme;
    const { projects, projectNames } = (0, get_project_names_1.getProjectsAndNames)(tree);
    let app = options.project;
    if (app && !projectNames.includes(app)) {
        throw new Error(`The provided project "${app}" is not an application in this workspace. Choose one of: ${projectNames.join(', ')}.`);
    }
    if (!app) {
        if (nonInteractive) {
            if (projectNames.length === 1) {
                app = projectNames[0];
            }
            else {
                throw new Error(`Could not determine which application to add the theme to. Pass "--project" to choose one of: ${projectNames.join(', ')}.`);
            }
        }
        else {
            const response = await (0, enquirer_1.prompt)({
                type: 'select',
                required: true,
                name: 'app',
                message: 'Choose which application you want to add the theme to:',
                choices: projectNames,
            });
            app = response.app;
        }
    }
    const project = projects.get(app);
    if (!project)
        return;
    let theme = options.theme;
    if (theme && !colors_1.themeNames.includes(theme)) {
        throw new Error(`The provided theme "${theme}" is not valid. Valid themes are: ${colors_1.themeNames.join(', ')}.`);
    }
    let stylesEntryPoint = options.stylesEntryPoint;
    let prefix = options.prefix;
    if (!nonInteractive) {
        const answers = await (0, enquirer_1.prompt)([
            {
                type: 'select',
                required: true,
                name: 'theme',
                message: 'Choose which theme to apply. You can always re-run this generator and add a custom prefix to add other themes.',
                choices: colors_1.themeNames,
            },
            {
                type: 'input',
                name: 'stylesEntryPoint',
                message: "Path to the styles entry point relative to the workspace root. If not provided the generator will do its best to find it and it will error if it can't.",
            },
            {
                type: 'input',
                name: 'prefix',
                message: "Prefix class name applied to your theme's style definitions: e.g., theme-zinc. Leave empty for global theme.",
            },
        ]);
        theme = answers.theme;
        stylesEntryPoint = answers.stylesEntryPoint;
        prefix = answers.prefix;
    }
    await (0, add_theme_to_application_styles_1.addThemeToApplicationStyles)(tree, {
        setupTailwindCss,
        project: project.name,
        theme: theme,
        stylesEntryPoint,
        prefix,
    }, project);
}
//# sourceMappingURL=generator.js.map