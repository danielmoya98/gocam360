import { ActiveDescendantKeyManager } from '@angular/cdk/a11y';
import * as i0 from '@angular/core';
import { InjectionToken, inject, forwardRef, Injector, input, booleanAttribute, linkedSignal, output, model, contentChildren, signal, computed, effect, untracked, afterNextRender, Directive, TemplateRef, ViewContainerRef, ElementRef, PLATFORM_ID } from '@angular/core';
import { takeUntilDestroyed } from '@angular/core/rxjs-interop';
import { NG_VALUE_ACCESSOR } from '@angular/forms';
import { startWith } from 'rxjs/operators';
import { isPlatformBrowser } from '@angular/common';

const BrnCommandItemToken = new InjectionToken('BrnCommandItemToken');
function provideBrnCommandItem(command) {
    return { provide: BrnCommandItemToken, useExisting: command };
}

const BrnCommandToken = new InjectionToken('BrnCommandToken');
function provideBrnCommand(command) {
    return { provide: BrnCommandToken, useExisting: command };
}
function injectBrnCommand() {
    return inject(BrnCommandToken);
}
const defaultConfig = {
    filter: (value, search) => value.toLowerCase().includes(search.toLowerCase()),
};
const BrnCommandConfigToken = new InjectionToken('BrnCommandConfig');
function provideBrnCommandConfig(config) {
    return { provide: BrnCommandConfigToken, useValue: { ...defaultConfig, ...config } };
}
function injectBrnCommandConfig() {
    return inject(BrnCommandConfigToken, { optional: true }) ?? defaultConfig;
}

const BRN_COMMAND_VALUE_ACCESSOR = {
    provide: NG_VALUE_ACCESSOR,
    useExisting: forwardRef(() => BrnCommand),
    multi: true,
};
class BrnCommand {
    static _id = 0;
    _injector = inject(Injector);
    _config = injectBrnCommandConfig();
    /** The id of the command */
    id = input(`brn-command-${++BrnCommand._id}`, ...(ngDevMode ? [{ debugName: "id" }] : /* istanbul ignore next */ []));
    /** A custom filter function to use when searching. */
    filter = input(this._config.filter, ...(ngDevMode ? [{ debugName: "filter" }] : /* istanbul ignore next */ []));
    /** Whether the command is disabled */
    disabled = input(false, { ...(ngDevMode ? { debugName: "disabled" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    _disabled = linkedSignal(this.disabled, ...(ngDevMode ? [{ debugName: "_disabled" }] : /* istanbul ignore next */ []));
    /** @internal The disabled state as a readonly signal */
    disabledState = this._disabled.asReadonly();
    /** when the selection has changed */
    valueChange = output();
    /** The current search query. */
    search = model('', ...(ngDevMode ? [{ debugName: "search" }] : /* istanbul ignore next */ []));
    /** @internal Access all the items within the command */
    items = contentChildren(BrnCommandItemToken, { ...(ngDevMode ? { debugName: "items" } : /* istanbul ignore next */ {}), descendants: true });
    /** @internal The key manager for managing active descendant */
    keyManager = new ActiveDescendantKeyManager(this.items, this._injector);
    _commandList = signal(undefined, ...(ngDevMode ? [{ debugName: "_commandList" }] : /* istanbul ignore next */ []));
    /** @internal The id of the command list, registered by BrnCommandList. Used by the input for aria-controls. */
    listId = computed(() => this._commandList()?.id(), ...(ngDevMode ? [{ debugName: "listId" }] : /* istanbul ignore next */ []));
    /** @internal Register the command list. Called by BrnCommandList in its constructor. */
    registerCommandList(list) {
        this._commandList.set(list);
    }
    _onChange;
    _onTouched;
    constructor() {
        this.keyManager
            .withVerticalOrientation()
            .withHomeAndEnd()
            .withWrap()
            .skipPredicate((item) => item.disabled || !item.visible());
        // When clearing the search input we also want to reset the active item to the first one
        effect(() => {
            const searchInput = this.search();
            untracked(() => {
                const activeItemIsVisible = this.keyManager.activeItem?.visible();
                if ((searchInput !== undefined && searchInput.length === 0) || !activeItemIsVisible) {
                    this.keyManager.setFirstItemActive();
                }
            });
        });
        this.keyManager.change.pipe(takeUntilDestroyed()).subscribe(() => {
            const value = this.keyManager.activeItem?.safeValue();
            if (value) {
                this.valueChange.emit(value);
            }
        });
        afterNextRender(() => {
            if (this.items().length) {
                this.keyManager.setActiveItem(0);
            }
        });
    }
    selectActiveItem() {
        this.keyManager.activeItem?.selected.emit();
    }
    /** CONTROL VALUE ACCESSOR */
    writeValue(value) {
        if (value) {
            this.search.set(value);
        }
    }
    registerOnChange(fn) {
        this._onChange = fn;
    }
    registerOnTouched(fn) {
        this._onTouched = fn;
    }
    setDisabledState(isDisabled) {
        this._disabled.set(isDisabled);
    }
    /** @nocollapse */ static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "21.2.16", ngImport: i0, type: BrnCommand, deps: [], target: i0.ɵɵFactoryTarget.Directive });
    /** @nocollapse */ static ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "17.2.0", version: "21.2.16", type: BrnCommand, isStandalone: true, selector: "[brnCommand]", inputs: { id: { classPropertyName: "id", publicName: "id", isSignal: true, isRequired: false, transformFunction: null }, filter: { classPropertyName: "filter", publicName: "filter", isSignal: true, isRequired: false, transformFunction: null }, disabled: { classPropertyName: "disabled", publicName: "disabled", isSignal: true, isRequired: false, transformFunction: null }, search: { classPropertyName: "search", publicName: "search", isSignal: true, isRequired: false, transformFunction: null } }, outputs: { valueChange: "valueChange", search: "searchChange" }, host: { listeners: { "keydown.enter": "selectActiveItem()" }, properties: { "id": "id()" } }, providers: [provideBrnCommand(BrnCommand), BRN_COMMAND_VALUE_ACCESSOR], queries: [{ propertyName: "items", predicate: BrnCommandItemToken, descendants: true, isSignal: true }], ngImport: i0 });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "21.2.16", ngImport: i0, type: BrnCommand, decorators: [{
            type: Directive,
            args: [{
                    selector: '[brnCommand]',
                    providers: [provideBrnCommand(BrnCommand), BRN_COMMAND_VALUE_ACCESSOR],
                    host: {
                        '[id]': 'id()',
                        '(keydown.enter)': 'selectActiveItem()',
                    },
                }]
        }], ctorParameters: () => [], propDecorators: { id: [{ type: i0.Input, args: [{ isSignal: true, alias: "id", required: false }] }], filter: [{ type: i0.Input, args: [{ isSignal: true, alias: "filter", required: false }] }], disabled: [{ type: i0.Input, args: [{ isSignal: true, alias: "disabled", required: false }] }], valueChange: [{ type: i0.Output, args: ["valueChange"] }], search: [{ type: i0.Input, args: [{ isSignal: true, alias: "search", required: false }] }, { type: i0.Output, args: ["searchChange"] }], items: [{ type: i0.ContentChildren, args: [i0.forwardRef(() => BrnCommandItemToken), { ...{
                            descendants: true,
                        }, isSignal: true }] }] } });

class BrnCommandEmpty {
    _templateRef = inject(TemplateRef);
    _viewContainerRef = inject(ViewContainerRef);
    _command = injectBrnCommand();
    /** Determine if the command has any visible items */
    _visible = computed(() => this._command.items().some((item) => item.visible()), ...(ngDevMode ? [{ debugName: "_visible" }] : /* istanbul ignore next */ []));
    constructor() {
        effect(() => {
            if (this._visible()) {
                this._viewContainerRef.clear();
            }
            else {
                this._viewContainerRef.createEmbeddedView(this._templateRef);
            }
        });
    }
    /** @nocollapse */ static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "21.2.16", ngImport: i0, type: BrnCommandEmpty, deps: [], target: i0.ɵɵFactoryTarget.Directive });
    /** @nocollapse */ static ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "14.0.0", version: "21.2.16", type: BrnCommandEmpty, isStandalone: true, selector: "[brnCommandEmpty]", ngImport: i0 });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "21.2.16", ngImport: i0, type: BrnCommandEmpty, decorators: [{
            type: Directive,
            args: [{
                    selector: '[brnCommandEmpty]',
                }]
        }], ctorParameters: () => [] });

class BrnCommandGroup {
    static _id = 0;
    /** The id of the command list */
    id = input(`brn-command-group-${++BrnCommandGroup._id}`, ...(ngDevMode ? [{ debugName: "id" }] : /* istanbul ignore next */ []));
    /** Get the items in the group */
    _items = contentChildren(BrnCommandItemToken, { ...(ngDevMode ? { debugName: "_items" } : /* istanbul ignore next */ {}), descendants: true });
    /** Determine if there are any visible items in the group */
    _visible = computed(() => this._items().some((item) => item.visible()), ...(ngDevMode ? [{ debugName: "_visible" }] : /* istanbul ignore next */ []));
    /** @nocollapse */ static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "21.2.16", ngImport: i0, type: BrnCommandGroup, deps: [], target: i0.ɵɵFactoryTarget.Directive });
    /** @nocollapse */ static ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "17.2.0", version: "21.2.16", type: BrnCommandGroup, isStandalone: true, selector: "[brnCommandGroup]", inputs: { id: { classPropertyName: "id", publicName: "id", isSignal: true, isRequired: false, transformFunction: null } }, host: { attributes: { "role": "group" }, properties: { "attr.data-hidden": "!_visible() ? true : null", "id": "id()" } }, queries: [{ propertyName: "_items", predicate: BrnCommandItemToken, descendants: true, isSignal: true }], ngImport: i0 });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "21.2.16", ngImport: i0, type: BrnCommandGroup, decorators: [{
            type: Directive,
            args: [{
                    selector: '[brnCommandGroup]',
                    host: {
                        role: 'group',
                        '[attr.data-hidden]': '!_visible() ? true : null',
                        '[id]': 'id()',
                    },
                }]
        }], propDecorators: { id: [{ type: i0.Input, args: [{ isSignal: true, alias: "id", required: false }] }], _items: [{ type: i0.ContentChildren, args: [i0.forwardRef(() => BrnCommandItemToken), { ...{
                            descendants: true,
                        }, isSignal: true }] }] } });

const BrnCommandInputToken = new InjectionToken('BrnCommandInputToken');
function provideBrnCommandInput(command) {
    return { provide: BrnCommandInputToken, useExisting: command };
}

class BrnCommandInput {
    static _id = 0;
    _el = inject(ElementRef);
    _command = injectBrnCommand();
    _initialId = `brn-command-input-${++BrnCommandInput._id}`;
    /** The id of the command input */
    id = input(this._initialId, { ...(ngDevMode ? { debugName: "id" } : /* istanbul ignore next */ {}), transform: (value) => value || this._initialId });
    _commandListId = this._command.listId;
    _disabled = this._command.disabledState;
    /** The id of the active option */
    _activeDescendant = signal(undefined, ...(ngDevMode ? [{ debugName: "_activeDescendant" }] : /* istanbul ignore next */ []));
    constructor() {
        this._command.keyManager.change
            .pipe(startWith(this._command.keyManager.activeItemIndex), takeUntilDestroyed())
            .subscribe(() => this._activeDescendant.set(this._command.keyManager.activeItem?.id()));
        effect(() => {
            const search = this._command.search();
            if (this._el.nativeElement.value !== search) {
                this._el.nativeElement.value = search;
            }
        });
    }
    /** Listen for changes to the input value */
    onInput() {
        this._command.search.set(this._el.nativeElement.value);
    }
    /** Listen for keydown events */
    onKeyDown(event) {
        this._command.keyManager.onKeydown(event);
    }
    /** @nocollapse */ static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "21.2.16", ngImport: i0, type: BrnCommandInput, deps: [], target: i0.ɵɵFactoryTarget.Directive });
    /** @nocollapse */ static ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "17.1.0", version: "21.2.16", type: BrnCommandInput, isStandalone: true, selector: "input[brnCommandInput]", inputs: { id: { classPropertyName: "id", publicName: "id", isSignal: true, isRequired: false, transformFunction: null } }, host: { attributes: { "role": "combobox", "aria-autocomplete": "list", "aria-haspopup": "listbox", "aria-expanded": "true", "type": "text", "autocomplete": "off", "autocorrect": "off", "spellcheck": "false" }, listeners: { "keydown": "onKeyDown($event)", "input": "onInput()" }, properties: { "id": "id()", "attr.aria-controls": "_commandListId()", "attr.aria-activedescendant": "_activeDescendant()", "attr.disabled": "_disabled() ? \"\" : null" } }, providers: [provideBrnCommandInput(BrnCommandInput)], ngImport: i0 });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "21.2.16", ngImport: i0, type: BrnCommandInput, decorators: [{
            type: Directive,
            args: [{
                    selector: 'input[brnCommandInput]',
                    providers: [provideBrnCommandInput(BrnCommandInput)],
                    host: {
                        '[id]': 'id()',
                        role: 'combobox',
                        'aria-autocomplete': 'list',
                        'aria-haspopup': 'listbox',
                        'aria-expanded': 'true',
                        '[attr.aria-controls]': '_commandListId()',
                        '[attr.aria-activedescendant]': '_activeDescendant()',
                        '[attr.disabled]': '_disabled() ? "" : null',
                        '(keydown)': 'onKeyDown($event)',
                        '(input)': 'onInput()',
                        type: 'text',
                        autocomplete: 'off',
                        autocorrect: 'off',
                        spellcheck: 'false',
                    },
                }]
        }], ctorParameters: () => [], propDecorators: { id: [{ type: i0.Input, args: [{ isSignal: true, alias: "id", required: false }] }] } });

class BrnCommandItem {
    static _id = 0;
    _platform = inject(PLATFORM_ID);
    _elementRef = inject(ElementRef);
    /** Access the command component */
    _command = injectBrnCommand();
    /** A unique id for the item */
    id = input(`brn-command-item-${++BrnCommandItem._id}`, ...(ngDevMode ? [{ debugName: "id" }] : /* istanbul ignore next */ []));
    /** The value this item represents. */
    value = input.required(...(ngDevMode ? [{ debugName: "value" }] : /* istanbul ignore next */ []));
    /** Whether the item is disabled. */
    // eslint-disable-next-line @typescript-eslint/naming-convention
    _disabled = input(false, { ...(ngDevMode ? { debugName: "_disabled" } : /* istanbul ignore next */ {}), alias: 'disabled',
        transform: booleanAttribute });
    /** Expose disabled as a value - used by the Highlightable interface */
    get disabled() {
        return this._disabled();
    }
    /** Whether the item is initialized, this is to prevent accessing the value-input before the component is initialized.
     * The brn-command-empty directive accesses the value before the component is initialized, which causes an error.
     */
    _initialized = signal(false, ...(ngDevMode ? [{ debugName: "_initialized" }] : /* istanbul ignore next */ []));
    /** Whether the item is selected. */
    _active = signal(false, ...(ngDevMode ? [{ debugName: "_active" }] : /* istanbul ignore next */ []));
    /** Emits when the item is selected. */
    selected = output();
    /** @internal Determine if this item is visible based on the current search query */
    visible = computed(() => {
        return this._command.filter()(this.safeValue(), this._command.search());
    }, ...(ngDevMode ? [{ debugName: "visible" }] : /* istanbul ignore next */ []));
    /** @internal Get the value of the item, with check if it has been initialized to avoid errors */
    safeValue = computed(() => {
        if (!this._initialized()) {
            return '';
        }
        return this.value();
    }, ...(ngDevMode ? [{ debugName: "safeValue" }] : /* istanbul ignore next */ []));
    /** @internal Get the display value */
    getLabel() {
        return this.safeValue();
    }
    /** @internal */
    setActiveStyles() {
        this._active.set(true);
        // ensure the item is in view
        if (isPlatformBrowser(this._platform)) {
            this._elementRef.nativeElement.scrollIntoView({ block: 'nearest' });
        }
    }
    /** @internal */
    setInactiveStyles() {
        this._active.set(false);
    }
    onClick() {
        this._command.keyManager.setActiveItem(this);
        this.selected.emit();
    }
    activate() {
        if (this._disabled()) {
            return;
        }
        this._command.keyManager.setActiveItem(this);
    }
    ngOnInit() {
        this._initialized.set(true);
    }
    /** @nocollapse */ static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "21.2.16", ngImport: i0, type: BrnCommandItem, deps: [], target: i0.ɵɵFactoryTarget.Directive });
    /** @nocollapse */ static ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "17.1.0", version: "21.2.16", type: BrnCommandItem, isStandalone: true, selector: "button[brnCommandItem]", inputs: { id: { classPropertyName: "id", publicName: "id", isSignal: true, isRequired: false, transformFunction: null }, value: { classPropertyName: "value", publicName: "value", isSignal: true, isRequired: true, transformFunction: null }, _disabled: { classPropertyName: "_disabled", publicName: "disabled", isSignal: true, isRequired: false, transformFunction: null } }, outputs: { selected: "selected" }, host: { attributes: { "type": "button", "role": "option", "tabIndex": "-1" }, listeners: { "click": "onClick()", "mouseenter": "activate()" }, properties: { "id": "id()", "attr.disabled": "_disabled() ? true : null", "attr.data-disabled": "_disabled() ? \"\" : null", "attr.data-value": "value()", "attr.data-hidden": "!visible() ? true : null", "attr.aria-selected": "_active()", "attr.data-selected": "_active() ? true : null" } }, providers: [provideBrnCommandItem(BrnCommandItem)], ngImport: i0 });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "21.2.16", ngImport: i0, type: BrnCommandItem, decorators: [{
            type: Directive,
            args: [{
                    selector: 'button[brnCommandItem]',
                    providers: [provideBrnCommandItem(BrnCommandItem)],
                    host: {
                        type: 'button',
                        role: 'option',
                        tabIndex: '-1',
                        '[id]': 'id()',
                        '[attr.disabled]': '_disabled() ? true : null',
                        '[attr.data-disabled]': '_disabled() ? "" : null',
                        '[attr.data-value]': 'value()',
                        '[attr.data-hidden]': '!visible() ? true : null',
                        '[attr.aria-selected]': '_active()',
                        '[attr.data-selected]': '_active() ? true : null',
                        '(click)': 'onClick()',
                        '(mouseenter)': 'activate()',
                    },
                }]
        }], propDecorators: { id: [{ type: i0.Input, args: [{ isSignal: true, alias: "id", required: false }] }], value: [{ type: i0.Input, args: [{ isSignal: true, alias: "value", required: true }] }], _disabled: [{ type: i0.Input, args: [{ isSignal: true, alias: "disabled", required: false }] }], selected: [{ type: i0.Output, args: ["selected"] }] } });

class BrnCommandList {
    static _id = 0;
    _command = injectBrnCommand();
    /** The id of the command list */
    id = input(`brn-command-list-${++BrnCommandList._id}`, ...(ngDevMode ? [{ debugName: "id" }] : /* istanbul ignore next */ []));
    /** Optional accessible name for the listbox. Usually not needed as the combobox names the widget. */
    ariaLabel = input(undefined, { ...(ngDevMode ? { debugName: "ariaLabel" } : /* istanbul ignore next */ {}), alias: 'aria-label' });
    constructor() {
        this._command.registerCommandList(this);
    }
    /** @nocollapse */ static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "21.2.16", ngImport: i0, type: BrnCommandList, deps: [], target: i0.ɵɵFactoryTarget.Directive });
    /** @nocollapse */ static ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "17.1.0", version: "21.2.16", type: BrnCommandList, isStandalone: true, selector: "[brnCommandList]", inputs: { id: { classPropertyName: "id", publicName: "id", isSignal: true, isRequired: false, transformFunction: null }, ariaLabel: { classPropertyName: "ariaLabel", publicName: "aria-label", isSignal: true, isRequired: false, transformFunction: null } }, host: { attributes: { "role": "listbox" }, properties: { "id": "id()", "attr.aria-label": "ariaLabel()" } }, ngImport: i0 });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "21.2.16", ngImport: i0, type: BrnCommandList, decorators: [{
            type: Directive,
            args: [{
                    selector: '[brnCommandList]',
                    host: {
                        role: 'listbox',
                        '[id]': 'id()',
                        '[attr.aria-label]': 'ariaLabel()',
                    },
                }]
        }], ctorParameters: () => [], propDecorators: { id: [{ type: i0.Input, args: [{ isSignal: true, alias: "id", required: false }] }], ariaLabel: [{ type: i0.Input, args: [{ isSignal: true, alias: "aria-label", required: false }] }] } });

class BrnCommandSeparator {
    _command = injectBrnCommand();
    /** Determine if the command has any search input */
    _hasSearchInput = computed(() => this._command.search().length > 0, ...(ngDevMode ? [{ debugName: "_hasSearchInput" }] : /* istanbul ignore next */ []));
    /** @nocollapse */ static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "21.2.16", ngImport: i0, type: BrnCommandSeparator, deps: [], target: i0.ɵɵFactoryTarget.Directive });
    /** @nocollapse */ static ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "14.0.0", version: "21.2.16", type: BrnCommandSeparator, isStandalone: true, selector: "[brnCommandSeparator]", host: { attributes: { "role": "presentation" }, properties: { "attr.data-hidden": "_hasSearchInput() ? true : null" } }, ngImport: i0 });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "21.2.16", ngImport: i0, type: BrnCommandSeparator, decorators: [{
            type: Directive,
            args: [{
                    selector: '[brnCommandSeparator]',
                    host: {
                        role: 'presentation',
                        '[attr.data-hidden]': '_hasSearchInput() ? true : null',
                    },
                }]
        }] });

const BrnCommandImports = [
    BrnCommand,
    BrnCommandEmpty,
    BrnCommandGroup,
    BrnCommandInput,
    BrnCommandItem,
    BrnCommandList,
    BrnCommandSeparator,
];

/**
 * Generated bundle index. Do not edit.
 */

export { BRN_COMMAND_VALUE_ACCESSOR, BrnCommand, BrnCommandEmpty, BrnCommandGroup, BrnCommandImports, BrnCommandInput, BrnCommandInputToken, BrnCommandItem, BrnCommandItemToken, BrnCommandList, BrnCommandSeparator, BrnCommandToken, injectBrnCommand, injectBrnCommandConfig, provideBrnCommand, provideBrnCommandConfig, provideBrnCommandInput, provideBrnCommandItem };
//# sourceMappingURL=spartan-ng-brain-command.mjs.map
